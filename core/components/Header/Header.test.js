import { render, prettyDOM, waitFor } from "@testing-library/react";
import { screen } from "@testing-library/dom";

test("header component and child  is render", () => {
  const toto = 0;

  expect(toto).toBe(0);
});
